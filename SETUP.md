# 🚀 Инструкция по запуску проекта АС ВСКЗ

## Шаг 1: Подготовка базы данных

### Вариант A: Использование существующей PostgreSQL

1. Убедитесь что PostgreSQL запущен
2. Обновите `.env` файл с вашими данными подключения:
```env
DATABASE_URL="postgresql://user:password@localhost:5432/warehouse_monitoring"
NEXTAUTH_SECRET="your-super-secret-key-change-this-in-production"
NEXTAUTH_URL="http://localhost:3000"
```

### Вариант B: Использование Docker

```bash
docker run --name warehouse-postgres \
  -e POSTGRES_USER=warehouse \
  -e POSTGRES_PASSWORD=warehouse123 \
  -e POSTGRES_DB=warehouse_monitoring \
  -p 5432:5432 \
  -d postgres:15
```

Затем обновите `.env`:
```env
DATABASE_URL="postgresql://warehouse:warehouse123@localhost:5432/warehouse_monitoring"
NEXTAUTH_SECRET="warehouse-secret-key-2024"
NEXTAUTH_URL="http://localhost:3000"
```

## Шаг 2: Установка зависимостей

```bash
npm install
```

## Шаг 3: Применение миграций БД

```bash
npx prisma migrate dev
```

Если миграция не создана, создайте её:
```bash
npx prisma migrate dev --name init
```

## Шаг 4: Создание тестовых пользователей

```bash
node scripts/seed-users.js
```

Будут созданы следующие пользователи:
- **admin@warehouse.ru** / admin123 (Администратор)
- **manager@warehouse.ru** / manager123 (Менеджер)
- **user@warehouse.ru** / user123 (Оператор)

## Шаг 5: (Опционально) Загрузка демо-данных

```bash
node scripts/seed-demo-data.js
```

Это создаст:
- 2 склада
- 4 зоны
- 12 локаций
- 20 SKU
- Остатки по локациям
- Нормы min/target/max

## Шаг 6: Запуск сервера разработки

```bash
npm run dev
```

Откройте браузер: http://localhost:3000

## 📋 Чек-лист запуска

- [ ] PostgreSQL запущен
- [ ] Файл `.env` настроен
- [ ] Зависимости установлены (`npm install`)
- [ ] Миграции применены (`npx prisma migrate dev`)
- [ ] Пользователи созданы (`node scripts/seed-users.js`)
- [ ] Демо-данные загружены (`node scripts/seed-demo-data.js`) - опционально
- [ ] Сервер запущен (`npm run dev`)
- [ ] Открыт http://localhost:3000

## 🔧 Устранение проблем

### Ошибка подключения к БД
```bash
# Проверьте что PostgreSQL запущен
# На Windows:
sc query postgresql-x64-15

# На Linux/Mac:
systemctl status postgresql
```

### Ошибка миграции
```bash
# Сбросьте БД и примените миграции заново
npx prisma migrate reset
```

### Ошибка "Module not found"
```bash
# Переустановите зависимости
rm -rf node_modules
rm package-lock.json
npm install
```

### Ошибка авторизации
```bash
# Пересоздайте пользователей
node scripts/seed-users.js
```

## 📊 Импорт данных через UI

1. Войдите как администратор (admin@warehouse.ru / admin123)
2. Перейдите на страницу http://localhost:3000/import
3. Скачайте шаблон CSV
4. Заполните данными
5. Загрузите файл

## 🎯 Тестирование функционала

### 1. KPI Dashboard
- Откройте `/dashboard`
- Проверьте отображение 5 KPI карточек
- Убедитесь что данные загружаются

### 2. Drill-down
- Кликните по столбцу склада → перейдет к зонам
- Кликните по столбцу зоны → перейдет к локациям
- Кликните по столбцу локации → перейдет к SKU
- Используйте breadcrumbs для навигации назад

### 3. Детальная панель
- Кликните по любому столбцу
- Справа откроется панель с деталями
- Проверьте топ SKU и проблемные позиции

### 4. Фильтры
- Используйте multi-select для складов
- Фильтруйте по ABC классу
- Попробуйте разные сортировки

### 5. Импорт
- Перейдите на `/import`
- Скачайте шаблон
- Загрузите тестовые данные
- Проверьте статистику и логи ошибок

## 🚀 Production deployment

### Vercel
```bash
vercel deploy
```

### Docker
```bash
docker build -t warehouse-monitoring .
docker run -p 3000:3000 warehouse-monitoring
```

### Переменные окружения для продакшн:
```env
DATABASE_URL="postgresql://..."
NEXTAUTH_SECRET="strong-random-secret"
NEXTAUTH_URL="https://your-domain.com"
```

## 📚 Дополнительно

### Prisma Studio (просмотр БД)
```bash
npx prisma studio
```

### Генерация типов после изменения схемы
```bash
npx prisma generate
```

### Форматирование кода
```bash
npm run lint
```

## 💡 Полезные команды

```bash
# Просмотр логов
npm run dev -- --debug

# Сборка для продакшн
npm run build
npm run start

# Проверка типов TypeScript
npx tsc --noEmit

# Prisma format schema
npx prisma format
```

Готово! Проект должен работать 🎉
