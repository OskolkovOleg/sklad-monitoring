# АС «Визуализация складской заполненности»

Система визуализации заполненности склада — столбчатая диаграмма остатков материалов.

## 📋 Описание

АС предназначена для автоматизации задач визуального мониторинга запасов на складах:
- Отображение текущих остатков и доли заполнения мощностей хранения
- Сравнение с минимальными/целевыми уровнями
- Выявление дефицитов и излишков
- Формирование отчётности и уведомлений

## 🏗️ Архитектура

Система состоит из трёх основных подсистем:

### 1. Подсистема интеграции и сбора данных
- Получение данных об остатках из WMS/ERP
- Поддержка расписаний обновления (≥ каждые 5 мин)
- Валидация и нормализация единиц измерения
- Учёт резервов и статусов запасов

### 2. Подсистема хранения и нормализации
- PostgreSQL база данных
- Справочники SKU, локаций, ёмкостей
- Хранение мин/целевых уровней
- Агрегаты по складам/зонам/локациям

### 3. Подсистема визуализации (Dashboard)
- Интерактивная столбчатая диаграмма
- Режимы группировки (склад/зона/ячейка/SKU)
- Фильтры и сортировка
- Drill-down навигация
- Цветовая индикация пороговых значений

## 🛠️ Технологический стек

- **Frontend**: Next.js 16, React 19, TypeScript, Tailwind CSS
- **Charts**: Recharts
- **Backend**: Next.js API Routes
- **Database**: PostgreSQL + Prisma ORM
- **Validation**: Zod
- **Icons**: Lucide React

## 🚀 Установка и запуск

### Требования

- Node.js 18+
- npm
- PostgreSQL 14+

### Установка

```bash
# Клонирование репозитория
git clone https://github.com/OskolkovOleg/sklad-monitoring.git
cd sklad-monitoring

# Установка зависимостей
npm install

# Настройка переменных окружения
# Создайте файл .env и укажите DATABASE_URL
# DATABASE_URL="postgresql://user:password@localhost:5432/sklad_monitoring"
```

### Настройка базы данных

```bash
# Создание миграции
npx prisma migrate dev --name init

# Генерация Prisma Client
npx prisma generate
```

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:3000

### Сборка для продакшн

```bash
npm run build
npm run start
```

## 📡 API Endpoints

### Управление складами

#### `POST /api/warehouses`
Синхронизация складов, зон и локаций

**Запрос:**
```json
{
  "warehouses": [
    {
      "code": "WH001",
      "name": "Основной склад",
      "description": "Главное хранилище",
      "zones": [
        {
          "code": "Z01",
          "name": "Зона А",
          "locations": [
            {
              "code": "L001",
              "name": "Локация A-1",
              "capacity": 1000,
              "unit": "шт"
            }
          ]
        }
      ]
    }
  ]
}
```

#### `GET /api/warehouses`
Получение списка складов с иерархией

### Управление SKU

#### `POST /api/sku`
Создание нового SKU

**Запрос:**
```json
{
  "code": "SKU001",
  "name": "Материал А",
  "category": "Металл",
  "supplier": "Поставщик 1",
  "abcClass": "A",
  "unit": "кг"
}
```

#### `GET /api/sku?page=1&pageSize=100&category=Металл`
Получение списка SKU с фильтрацией

### Управление остатками

#### `POST /api/inventory`
Импорт данных об остатках

**Запрос:**
```json
{
  "data": [
    {
      "skuCode": "SKU001",
      "locationCode": "WH001-Z01-L001",
      "quantity": 500,
      "reservedQty": 50,
      "unavailableQty": 10,
      "status": "available"
    }
  ]
}
```

**Ответ:**
```json
{
  "success": true,
  "imported": 1,
  "errors": []
}
```

#### `GET /api/inventory?page=1&pageSize=100`
Получение данных об остатках

### Управление нормами

#### `POST /api/inventory/norms`
Импорт норм запасов

**Запрос:**
```json
{
  "skuNorms": [
    {
      "skuCode": "SKU001",
      "minLevel": 100,
      "targetLevel": 500,
      "maxLevel": 1000
    }
  ],
  "locationNorms": [
    {
      "locationCode": "L001",
      "minLevel": 50,
      "targetLevel": 200,
      "maxLevel": 500
    }
  ]
}
```

### Получение агрегаций

#### `GET /api/aggregations`
Получение агрегированных данных для Dashboard

**Параметры:**
- `entityType` - тип сущности (warehouse/zone/location/sku)
- `warehouseId` - фильтр по складу
- `zoneId` - фильтр по зоне
- `category` - фильтр по категории
- `supplier` - фильтр по поставщику
- `abcClass` - фильтр по ABC классу (A/B/C)
- `status` - фильтр по статусу (green/yellow/red/gray)
- `search` - поиск по коду или названию
- `sortField` - поле сортировки
- `sortOrder` - порядок сортировки (asc/desc)
- `page` - номер страницы
- `pageSize` - размер страницы

**Пример:**
```
GET /api/aggregations?entityType=warehouse&status=red&sortField=fillPercentage&sortOrder=asc
```

## 🎨 Цветовая индикация

Система использует цветовую индикацию для отображения статуса заполненности:

- 🟢 **Зелёный** — нормальный уровень (≥ target)
- 🟡 **Жёлтый** — требует внимания (между min и target)
- 🔴 **Красный** — критический уровень (< min)
- ⚪ **Серый** — нет данных о нормах

## 📊 Использование Dashboard

1. Откройте Dashboard по адресу `/dashboard`
2. Выберите режим отображения: "Количество" или "Заполненность %"
3. Используйте фильтры для поиска нужных данных
4. Кликните по столбцу для drill-down в детализацию
5. Используйте хлебные крошки для навигации назад

## 🔄 Обновление данных

Для обновления агрегаций после изменения данных система автоматически пересчитывает показатели при:
- Импорте остатков
- Импорте норм
- Синхронизации справочников

## 📝 Схема базы данных

Основные таблицы:
- `Warehouse` - склады
- `Zone` - зоны складов
- `Location` - локации (ячейки)
- `SKU` - товары/материалы
- `Inventory` - остатки
- `SKUNorm` - нормы по SKU
- `LocationNorm` - нормы по локациям
- `Aggregation` - агрегированные данные
- `ErrorLog` - журнал ошибок

## 🧪 Разработка

### Структура проекта

```
├── app/
│   ├── api/              # API endpoints
│   │   ├── aggregations/ # Агрегации
│   │   ├── inventory/    # Остатки и нормы
│   │   ├── sku/          # SKU
│   │   └── warehouses/   # Склады
│   ├── dashboard/        # Dashboard страница
│   ├── layout.tsx
│   └── page.tsx          # Главная страница
├── components/
│   ├── dashboard/        # Компоненты Dashboard
│   └── ui/               # UI компоненты
├── lib/
│   ├── db/               # Prisma client
│   ├── services/         # Бизнес-логика
│   ├── utils/            # Утилиты
│   └── validations/      # Zod схемы
├── prisma/
│   └── schema.prisma     # Схема БД
├── types/                # TypeScript типы
└── zdocs/                # Документация
```

### Git Workflow

Проект использует Git Flow:
- `main` - продакшн ветка
- `develop` - ветка разработки
- `feature/*` - новые фичи
- `fix/*` - исправления

Подробнее см. `zdocs/git-workflow.md`

## 📄 Лицензия

АО «Консист ОС»

Разработано в соответствии с:
- ГОСТ Р 59793-2021 «КСАС. Автоматизированные системы. Стадии создания»
- ГОСТ 34.602-2020 «Техническое задание на создание автоматизированной системы»

## 👥 Авторы

Разработано для АО «Консист ОС» в рамках программы трансформации финансово-экономического блока ИТ Госкорпорации «Росатом».
