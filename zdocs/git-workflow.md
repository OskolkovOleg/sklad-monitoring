# 🌱 Git workflow для FitFly

Этот гайд описывает, как мы работаем с Git, чтобы:
- избежать хаоса,
- стабильно выкатывать релизы,
- использовать автодеплой на прод.

---

## 🔀 Структура веток

| Ветка        | Назначение                          |
|--------------|-------------------------------------|
| `main`       | ✅ Продакшн. Только стабильный код. Автодеплой. |
| `develop`    | 🛠 Ветка разработки. Все фичи мержим сюда. |
| `feature/*`  | Новые фичи (например, `feature/payments-ui`) |
| `fix/*`      | Исправления багов                   |
| `test/*`     | Эксперименты, прототипы             |

---

## 📦 Как начать новую фичу

```bash
# Переключаемся на develop
# Обновляем develop
# Создаем фичу
git checkout develop
git pull origin develop
git checkout -b feature/название

Пример:
git checkout -b feature/referral-paywall


✅ Завершение фичи
# Добавляем изменения
git add .
git commit -m "Добавляет реферальный paywall"

# Отправляем ветку на GitHub
git push -u origin feature/referral-paywall


Создаём Pull Request в develop
  

🚀 Релиз (выкатить на прод)
 
# Переключаемся на main
# Обновляем
# Сливаем изменения из develop
# Отправляем в репозиторий

git checkout main
git pull origin main
git merge develop
git push origin main


Чтобы задеплоить


🛠 Полезные команды
Обновить develop перед началом работы:

bash
Копировать
Редактировать
git checkout develop
git pull origin develop
Удалить локальные фичи после мержа:

bash
Копировать
Редактировать
git branch -d feature/название
Удалить ветку с GitHub:

bash
Копировать
Редактировать
git push origin --delete feature/название
🧪 Рекомендации
Один PR — одна фича.

Не коммить в main напрямую.

Делай git pull перед началом работы.

Держи .env.production только в main.

🔐 Автодеплой
Пуш в main → GitHub Actions → SSH на сервер → git pull && npm run build && pm2 restart.

Секреты:
  

SSH_PRIVATE_KEY

SERVER_HOST

Хочешь добавить фичу? Создай feature/название, сделай PR в develop.
Хочешь выкатить релиз? Мерж develop в main.
Чисто. Просто. Работает.

git checkout develop
git rebase main
git push origin develop --force-with-lease


ngrok http 3000


# npm run build
# npm run start